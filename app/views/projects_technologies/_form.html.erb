<%= form_for(@projects_technology) do |f| %>
  <% if @projects_technology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projects_technology.errors.count, "error") %> prohibited this projects_technology from being saved:</h2>

      <ul>
      <% @projects_technology.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project %><br>
    <%= f.text_field :project %>
  </div>
  <div class="field">
    <%= f.label :technology %><br>
    <%= f.text_field :technology %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
